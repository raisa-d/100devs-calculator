class Calculator {
    add = (a, b) => a + b;
    subtract = (a, b) => a - b;
    multiply = (a, b) => a * b;
    divide = (a, b) => a / b;
}

let calc = new Calculator()

/*
need to:
1. make it so that you can click on it on document and actually use it visually
2. allow user to make a calculation with more than 2 numbers if desired
*/